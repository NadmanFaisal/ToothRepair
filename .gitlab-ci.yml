stages:
  - build

# Build the frontend
frontend-build:
  stage: build
  image: node:20-alpine
  script:
    - cd client
    - npm install
    - npm run build

# Build th backend
backend-build:
  stage: build
  image: node:20-alpine
  script:
    - cd server
    - npm install