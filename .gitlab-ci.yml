image: node:20-alpine

cache:
  key: ${CI_COMMIT_REF_SLUG}
  paths:
    - client/node_modules/
    - server/node_modules/

stages:
  - build

# Build the frontend
frontend-build:
  stage: build
  script:
    - cd client
    - npm install
    - npm run build
  artifacts:
    paths:
      - client/dist

# Build the backend
backend-build:
  stage: build
  script:
    - cd server
    - npm install
  artifacts:
    paths:
      - server